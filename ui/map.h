#ifndef MAP_H
#define MAP_H

// Loads the map background
void load_map();

// Updates the map and renders the GPS marker
void update_map();

// Renders GPS location on the map
void render_gps_location();

// Clears the screen and displays the map UI
void open_map();

#endif